apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "chart.fullname" . }}-project-admin-role
  labels:
  {{- include "chart.labels" . | nindent 4 }}
rules:
- apiGroups:
  - bubu.sh
  resources:
  - projects
  verbs:
  - '*'
- apiGroups:
  - bubu.sh
  resources:
  - projects/status
  verbs:
  - get

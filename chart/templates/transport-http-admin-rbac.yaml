apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "chart.fullname" . }}-transport-http-admin-role
  labels:
  {{- include "chart.labels" . | nindent 4 }}
rules:
- apiGroups:
  - transport.bubu.sh
  resources:
  - https
  verbs:
  - '*'
- apiGroups:
  - transport.bubu.sh
  resources:
  - https/status
  verbs:
  - get

---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: modelconfigs.bubu.sh
spec:
  group: bubu.sh
  names:
    kind: ModelConfig
    listKind: ModelConfigList
    plural: modelconfigs
    shortNames:
    - mc
    singular: modelconfig
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.modelRef.name
      name: ModelRef
      type: string
    - jsonPath: .spec.modelRef.version
      name: Version
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ModelConfig is the Schema for the modelconfigs API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ModelConfigSpec defines the desired state of ModelConfig.
            properties:
              additionalConfig:
                description: AdditionalConfig holds any supplementary configuration
                  for the model binding
                type: object
                x-kubernetes-preserve-unknown-fields: true
              credentials:
                description: Credentials contains authentication and configuration
                  details
                properties:
                  envVars:
                    additionalProperties:
                      type: string
                    description: EnvVars is a map of environment variables to be set
                      in the model container
                    type: object
                  files:
                    description: Files is a list of file mounts to be used for configuration
                    items:
                      description: FileMount defines a file to be mounted
                      properties:
                        mountPath:
                          description: MountPath is the path where the file should
                            be mounted
                          minLength: 1
                          pattern: ^(/[^/\0]+)+$|^/[^/\0]*$
                          type: string
                        secretRef:
                          description: SecretRef references the secret containing
                            the file content
                          properties:
                            key:
                              description: Key is the key in the secret to reference
                              minLength: 1
                              type: string
                            name:
                              description: Name is the name of the secret
                              minLength: 1
                              type: string
                          required:
                          - key
                          - name
                          type: object
                      required:
                      - mountPath
                      - secretRef
                      type: object
                    type: array
                  secrets:
                    description: Secrets is a list of secret references to be used
                      for authentication
                    items:
                      description: SecretRef defines a reference to a secret with
                        a specific key
                      properties:
                        key:
                          description: Key is the key in the secret to reference
                          minLength: 1
                          type: string
                        name:
                          description: Name is the name of the secret
                          minLength: 1
                          type: string
                      required:
                      - key
                      - name
                      type: object
                    type: array
                type: object
              endpoint:
                description: |-
                  Endpoint is the API endpoint for the model
                  This allows overriding the default endpoint specified in the Model resource
                minLength: 1
                pattern: ^(http|https)://.*
                type: string
              modelRef:
                description: |-
                  ModelRef references a predefined Model resource in the cluster
                  This structure contains the name of an existing Model custom resource
                  and optionally a specific version to use
                properties:
                  name:
                    description: Name is the name of the Model custom resource
                    minLength: 1
                    type: string
                  version:
                    description: |-
                      Version is the specific version of the model to use
                      This can be used to pin to a specific model version when multiple
                      versions are available in the model catalog
                    type: string
                required:
                - name
                type: object
            required:
            - modelRef
            type: object
          status:
            description: ModelConfigStatus defines the observed state of ModelConfig.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the model configuration's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastUpdated:
                description: LastUpdated represents the last time the configuration
                  was updated
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}

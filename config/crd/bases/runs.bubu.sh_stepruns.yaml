---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: stepruns.runs.bubu.sh
spec:
  group: runs.bubu.sh
  names:
    kind: StepRun
    listKind: StepRunList
    plural: stepruns
    shortNames:
    - step
    singular: steprun
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              deadline:
                description: ABI contract environment
                type: string
              engramRef:
                description: Engram execution details
                type: string
              idempotencyToken:
                type: string
              image:
                type: string
              input:
                type: object
                x-kubernetes-preserve-unknown-fields: true
              mode:
                description: Execution configuration
                type: string
              retryPolicy:
                properties:
                  backoff:
                    type: string
                  baseDelay:
                    type: string
                  circuitBreaker:
                    description: Circuit breaker configuration
                    properties:
                      failureConditions:
                        description: What constitutes a failure (HTTP codes, exit
                          codes, etc.)
                        items:
                          description: FailureCondition defines what constitutes a
                            failure
                          properties:
                            exceptionPattern:
                              description: 'For exception: exception patterns'
                              type: string
                            exitCodePattern:
                              description: 'For exitCode: container exit codes (e.g.,
                                "!=0")'
                              type: string
                            httpStatusPattern:
                              description: 'For httpStatus: HTTP status codes (e.g.,
                                "5xx", ">=400")'
                              type: string
                            includeTimeouts:
                              description: 'For timeout: consider timeouts as failures'
                              type: boolean
                            type:
                              type: string
                          required:
                          - type
                          type: object
                        type: array
                      failureThreshold:
                        description: Failure threshold before opening circuit
                        format: int32
                        type: integer
                      fallbackAction:
                        description: Fallback action when circuit is open
                        properties:
                          engramRef:
                            description: 'For type=engram: reference to fallback engram'
                            type: string
                          type:
                            type: string
                          value:
                            description: 'For type=value: static fallback value'
                            type: object
                            x-kubernetes-preserve-unknown-fields: true
                        required:
                        - type
                        type: object
                      resetTimeout:
                        description: Reset timeout when circuit is open
                        type: string
                      successThreshold:
                        description: Success threshold to close circuit from half-open
                        format: int32
                        type: integer
                      timeWindow:
                        description: Time window to evaluate failures
                        type: string
                    type: object
                  maxDelay:
                    type: string
                  maxRetries:
                    type: integer
                type: object
              stepId:
                type: string
              storyRunRef:
                type: string
              timeout:
                type: string
                x-kubernetes-validations:
                - message: timeout must be a valid duration
                  rule: size(self) == 0 || self.matches('^([0-9]+(\\.[0-9]+)?(ms|s|m|h))+$')
            required:
            - stepId
            - storyRunRef
            type: object
            x-kubernetes-validations:
            - message: storyRunRef is required
              rule: size(self.storyRunRef) > 0
            - message: storyRunRef must be valid DNS-1123 label
              rule: self.storyRunRef.matches('^[a-z0-9]([-a-z0-9]*[a-z0-9])?$')
            - message: stepId is required
              rule: size(self.stepId) > 0
            - message: engramRef must be valid name or ns/name format
              rule: '!has(self.engramRef) || self.engramRef.matches(''^([a-z0-9]([-a-z0-9]*[a-z0-9])?/)?[a-z0-9]([-a-z0-9]*[a-z0-9])?$'')'
          status:
            properties:
              artifacts:
                items:
                  properties:
                    name:
                      type: string
                    sha256:
                      type: string
                    size:
                      format: int64
                      type: integer
                    ttl:
                      type: string
                    uri:
                      type: string
                  required:
                  - name
                  - sha256
                  - size
                  - uri
                  type: object
                type: array
              dependencies:
                description: Dependencies and triggering (simplified communication
                  pattern)
                items:
                  type: string
                type: array
              error:
                type: object
                x-kubernetes-preserve-unknown-fields: true
              exitClass:
                type: string
              exitCode:
                format: int32
                type: integer
              finishedAt:
                type: string
              logsURI:
                description: Observability
                type: string
              output:
                description: ABI contract outputs - Enhanced for large data handling
                type: object
                x-kubernetes-preserve-unknown-fields: true
              pauseInfo:
                description: Pause information
                properties:
                  externalTriggerStatus:
                    description: External trigger status
                    type: string
                  pausedAt:
                    description: When the step was paused
                    format: date-time
                    type: string
                  reason:
                    description: Reason for pausing
                    type: string
                  resumeAt:
                    description: Expected resume time (for timeout-based resume)
                    format: date-time
                    type: string
                type: object
              phase:
                type: string
              podName:
                type: string
              retries:
                type: integer
              startedAt:
                description: Execution metadata
                type: string
              traceID:
                type: string
              triggers:
                items:
                  type: string
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}

resources:
# All RBAC will be applied under this service account in
# the deployment namespace. You may comment out this resource
# if your manager will use a service account that exists at
# runtime. Be sure to update RoleBinding and ClusterRoleBinding
# subjects if changing service account names.
- service_account.yaml
- role.yaml
- role_binding.yaml
- leader_election_role.yaml
- leader_election_role_binding.yaml
# The following RBAC configurations are used to protect
# the metrics endpoint with authn/authz. These configurations
# ensure that only authorized users and service accounts
# can access the metrics endpoint. Comment the following
# permissions if you want to disable this protection.
# More info: https://book.kubebuilder.io/reference/metrics.html
- metrics_auth_role.yaml
- metrics_auth_role_binding.yaml
- metrics_reader_role.yaml
# For each CRD, "Admin", "Editor" and "Viewer" roles are scaffolded by
# default, aiding admins in cluster management. Those roles are
# not used by the bobrapet itself. You can comment the following lines
# if you do not want those helpers be installed with your Project.
- catalog_impulsetemplate_admin_role.yaml
- catalog_impulsetemplate_editor_role.yaml
- catalog_impulsetemplate_viewer_role.yaml
- catalog_engramtemplate_admin_role.yaml
- catalog_engramtemplate_editor_role.yaml
- catalog_engramtemplate_viewer_role.yaml
- runs_steprun_admin_role.yaml
- runs_steprun_editor_role.yaml
- runs_steprun_viewer_role.yaml
- runs_storyrun_admin_role.yaml
- runs_storyrun_editor_role.yaml
- runs_storyrun_viewer_role.yaml
- impulse_admin_role.yaml
- impulse_editor_role.yaml
- impulse_viewer_role.yaml
- engram_admin_role.yaml
- engram_editor_role.yaml
- engram_viewer_role.yaml
- story_admin_role.yaml
- story_editor_role.yaml
- story_viewer_role.yaml


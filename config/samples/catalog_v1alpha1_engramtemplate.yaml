apiVersion: catalog.bubustack.io/v1alpha1
kind: EngramTemplate
metadata:
  labels:
    app.kubernetes.io/name: bobrapet
    app.kubernetes.io/managed-by: kustomize
  name: engramtemplate-sample
spec:
  # Template sample. Populate this spec with a valid EngramTemplate before applying.

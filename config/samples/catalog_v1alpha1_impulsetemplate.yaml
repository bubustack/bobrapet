apiVersion: catalog.bubustack.io/v1alpha1
kind: ImpulseTemplate
metadata:
  labels:
    app.kubernetes.io/name: bobrapet
    app.kubernetes.io/managed-by: kustomize
  name: impulsetemplate-sample
spec:
  # Template sample. Populate this spec with a valid ImpulseTemplate before applying.

apiVersion: transport.bubustack.io/v1alpha1
kind: Transport
metadata:
  labels:
    app.kubernetes.io/name: bobrapet
    app.kubernetes.io/managed-by: kustomize
  name: transport-sample
spec:
  # Template sample. Populate this spec with a valid Transport before applying.

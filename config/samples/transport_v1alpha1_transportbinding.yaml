apiVersion: transport.bubustack.io/v1alpha1
kind: TransportBinding
metadata:
  labels:
    app.kubernetes.io/name: bobrapet
    app.kubernetes.io/managed-by: kustomize
  name: transportbinding-sample
spec:
  # Template sample. Populate this spec with a valid TransportBinding before applying.
